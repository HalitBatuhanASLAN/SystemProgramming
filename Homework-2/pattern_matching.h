#ifndef PARTTERN_MATCHING_H
#define PARTTERN_MATCHING_H

/*
    checks if a file contains the searching pattern as a substring
*/
int is_match_pattern(const char *file_name, const char *pattern);

#endif